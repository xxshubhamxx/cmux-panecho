name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  workflow-guard-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Validate WarpBuild runner guards
        run: ./tests/test_ci_self_hosted_guard.sh

      - name: Validate create-dmg version pinning
        run: ./tests/test_ci_create_dmg_pinned.sh

      - name: Validate unit-test SwiftPM retry guard
        run: ./tests/test_ci_unit_test_spm_retry.sh

      - name: Validate unit-test shard timeout guard
        run: bash ./tests/test_ci_unit_shard_timeout.sh

      - name: Validate retry output gating
        run: bash ./tests/test_ci_retry_output_gating.sh

      - name: Validate cmux scheme test configuration
        run: ./tests/test_ci_scheme_testaction_debug.sh

      - name: Validate GhosttyKit checksum verification
        run: ./tests/test_ci_ghosttykit_checksum_verification.sh

      - name: Validate macOS compat test slice
        run: ./tests/test_ci_macos_compat_test_slice.sh

      - name: Validate release asset guard
        run: node scripts/release_asset_guard.test.js

      - name: Validate current GhosttyKit checksum pin
        run: ./tests/test_ci_ghosttykit_checksum_present.sh

  remote-daemon-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Setup Go
        uses: actions/setup-go@d35c59abb061a4a6fb18e82ac0862c26744d6ab5 # v5.5.0
        with:
          go-version-file: daemon/remote/go.mod

      - name: Run remote daemon tests
        working-directory: daemon/remote
        run: go test ./...

      - name: Validate remote daemon release assets
        run: ./tests/test_remote_daemon_release_assets.sh

  web-typecheck:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@3d267786b128fe76c2f16a390aa2448b815359f3 # v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Typecheck
        run: bun tsc --noEmit

  tests-shard-1-attempt-1:
    # Never run WarpBuild jobs for fork pull requests (avoid billing on external PRs).
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    continue-on-error: true
    runs-on: warp-macos-15-arm64-6x
    timeout-minutes: 10
    outputs:
      test_started: ${{ steps.unit-test-shard.outputs.test_started }}
      completed: ${{ steps.mark-attempt-complete.outputs.value }}
      passed: ${{ steps.mark-attempt-pass.outputs.value }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive
      - name: Run unit test shard
        id: unit-test-shard
        uses: ./.github/actions/run-unit-test-shard
        with:
          shard_index: "0"
          shard_count: "6"
      - name: Run bundled Ghostty theme picker helper regression
        run: |
          set -euo pipefail
          CMUX_SOURCE_PACKAGES_DIR="$PWD/.ci-source-packages" \
            ./tests/test_bundled_ghostty_theme_picker_helper.sh
      - name: Run CLI version memory guard regression
        run: |
          set -euo pipefail

          CLI_BIN="$(
            find "$HOME/Library/Developer/Xcode/DerivedData" -path "*/Build/Products/Debug/cmux" -exec stat -f '%m %N' {} \; \
              | sort -nr \
              | head -1 \
              | cut -d' ' -f2-
          )"
          if [ -z "${CLI_BIN:-}" ] || [ ! -x "$CLI_BIN" ]; then
            echo "cmux CLI binary not found in DerivedData" >&2
            exit 1
          fi

          CMUX_CLI_BIN="$CLI_BIN" python3 tests/test_cli_version_memory_guard.py
      - name: Mark unit test shard attempt completed
        if: ${{ always() && !cancelled() }}
        id: mark-attempt-complete
        run: echo "value=true" >> "$GITHUB_OUTPUT"
      - name: Mark unit test shard attempt passed
        if: ${{ success() }}
        id: mark-attempt-pass
        run: echo "value=true" >> "$GITHUB_OUTPUT"

  tests-shard-1-attempt-2:
    if: ${{ always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && needs.tests-shard-1-attempt-1.outputs.completed != 'true' && needs.tests-shard-1-attempt-1.outputs.passed != 'true' }}
    needs:
      - tests-shard-1-attempt-1
    continue-on-error: true
    runs-on: warp-macos-15-arm64-6x
    timeout-minutes: 10
    outputs:
      test_started: ${{ steps.unit-test-shard.outputs.test_started }}
      completed: ${{ steps.mark-attempt-complete.outputs.value }}
      passed: ${{ steps.mark-attempt-pass.outputs.value }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive
      - name: Run unit test shard
        id: unit-test-shard
        uses: ./.github/actions/run-unit-test-shard
        with:
          shard_index: "0"
          shard_count: "6"
      - name: Run bundled Ghostty theme picker helper regression
        run: |
          set -euo pipefail
          CMUX_SOURCE_PACKAGES_DIR="$PWD/.ci-source-packages" \
            ./tests/test_bundled_ghostty_theme_picker_helper.sh
      - name: Run CLI version memory guard regression
        run: |
          set -euo pipefail

          CLI_BIN="$(
            find "$HOME/Library/Developer/Xcode/DerivedData" -path "*/Build/Products/Debug/cmux" -exec stat -f '%m %N' {} \; \
              | sort -nr \
              | head -1 \
              | cut -d' ' -f2-
          )"
          if [ -z "${CLI_BIN:-}" ] || [ ! -x "$CLI_BIN" ]; then
            echo "cmux CLI binary not found in DerivedData" >&2
            exit 1
          fi

          CMUX_CLI_BIN="$CLI_BIN" python3 tests/test_cli_version_memory_guard.py
      - name: Mark unit test shard attempt completed
        if: ${{ always() && !cancelled() }}
        id: mark-attempt-complete
        run: echo "value=true" >> "$GITHUB_OUTPUT"
      - name: Mark unit test shard attempt passed
        if: ${{ success() }}
        id: mark-attempt-pass
        run: echo "value=true" >> "$GITHUB_OUTPUT"

  tests-shard-1:
    if: ${{ always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs:
      - tests-shard-1-attempt-1
      - tests-shard-1-attempt-2
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Verify unit test shard passed
        run: |
          scripts/verify_retry_result.sh \
            "Unit test shard" \
            "${{ needs.tests-shard-1-attempt-1.result }}" \
            "${{ needs.tests-shard-1-attempt-1.outputs.completed }}" \
            "${{ needs.tests-shard-1-attempt-1.outputs.passed }}" \
            "${{ needs.tests-shard-1-attempt-2.result }}" \
            "${{ needs.tests-shard-1-attempt-2.outputs.completed }}" \
            "${{ needs.tests-shard-1-attempt-2.outputs.passed }}"

  tests-shard-2-attempt-1:
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    continue-on-error: true
    runs-on: warp-macos-15-arm64-6x
    timeout-minutes: 10
    outputs:
      test_started: ${{ steps.unit-test-shard.outputs.test_started }}
      completed: ${{ steps.unit-test-shard.outputs.completed }}
      passed: ${{ steps.unit-test-shard.outputs.passed }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive
      - name: Run unit test shard
        id: unit-test-shard
        uses: ./.github/actions/run-unit-test-shard
        with:
          shard_index: "1"
          shard_count: "6"

  tests-shard-2-attempt-2:
    if: ${{ always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && needs.tests-shard-2-attempt-1.outputs.completed != 'true' && needs.tests-shard-2-attempt-1.outputs.passed != 'true' }}
    needs:
      - tests-shard-2-attempt-1
    continue-on-error: true
    runs-on: warp-macos-15-arm64-6x
    timeout-minutes: 10
    outputs:
      test_started: ${{ steps.unit-test-shard.outputs.test_started }}
      completed: ${{ steps.unit-test-shard.outputs.completed }}
      passed: ${{ steps.unit-test-shard.outputs.passed }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive
      - name: Run unit test shard
        id: unit-test-shard
        uses: ./.github/actions/run-unit-test-shard
        with:
          shard_index: "1"
          shard_count: "6"

  tests-shard-2:
    if: ${{ always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs:
      - tests-shard-2-attempt-1
      - tests-shard-2-attempt-2
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Verify unit test shard passed
        run: |
          scripts/verify_retry_result.sh \
            "Unit test shard" \
            "${{ needs.tests-shard-2-attempt-1.result }}" \
            "${{ needs.tests-shard-2-attempt-1.outputs.completed }}" \
            "${{ needs.tests-shard-2-attempt-1.outputs.passed }}" \
            "${{ needs.tests-shard-2-attempt-2.result }}" \
            "${{ needs.tests-shard-2-attempt-2.outputs.completed }}" \
            "${{ needs.tests-shard-2-attempt-2.outputs.passed }}"

  tests-shard-3-attempt-1:
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    continue-on-error: true
    runs-on: warp-macos-15-arm64-6x
    timeout-minutes: 10
    outputs:
      test_started: ${{ steps.unit-test-shard.outputs.test_started }}
      completed: ${{ steps.unit-test-shard.outputs.completed }}
      passed: ${{ steps.unit-test-shard.outputs.passed }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive
      - name: Run unit test shard
        id: unit-test-shard
        uses: ./.github/actions/run-unit-test-shard
        with:
          shard_index: "2"
          shard_count: "6"

  tests-shard-3-attempt-2:
    if: ${{ always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && needs.tests-shard-3-attempt-1.outputs.completed != 'true' && needs.tests-shard-3-attempt-1.outputs.passed != 'true' }}
    needs:
      - tests-shard-3-attempt-1
    continue-on-error: true
    runs-on: warp-macos-15-arm64-6x
    timeout-minutes: 10
    outputs:
      test_started: ${{ steps.unit-test-shard.outputs.test_started }}
      completed: ${{ steps.unit-test-shard.outputs.completed }}
      passed: ${{ steps.unit-test-shard.outputs.passed }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive
      - name: Run unit test shard
        id: unit-test-shard
        uses: ./.github/actions/run-unit-test-shard
        with:
          shard_index: "2"
          shard_count: "6"

  tests-shard-3:
    if: ${{ always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs:
      - tests-shard-3-attempt-1
      - tests-shard-3-attempt-2
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Verify unit test shard passed
        run: |
          scripts/verify_retry_result.sh \
            "Unit test shard" \
            "${{ needs.tests-shard-3-attempt-1.result }}" \
            "${{ needs.tests-shard-3-attempt-1.outputs.completed }}" \
            "${{ needs.tests-shard-3-attempt-1.outputs.passed }}" \
            "${{ needs.tests-shard-3-attempt-2.result }}" \
            "${{ needs.tests-shard-3-attempt-2.outputs.completed }}" \
            "${{ needs.tests-shard-3-attempt-2.outputs.passed }}"

  tests-shard-4-attempt-1:
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    continue-on-error: true
    runs-on: warp-macos-15-arm64-6x
    timeout-minutes: 10
    outputs:
      test_started: ${{ steps.unit-test-shard.outputs.test_started }}
      completed: ${{ steps.unit-test-shard.outputs.completed }}
      passed: ${{ steps.unit-test-shard.outputs.passed }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive
      - name: Run unit test shard
        id: unit-test-shard
        uses: ./.github/actions/run-unit-test-shard
        with:
          shard_index: "3"
          shard_count: "6"

  tests-shard-4-attempt-2:
    if: ${{ always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && needs.tests-shard-4-attempt-1.outputs.completed != 'true' && needs.tests-shard-4-attempt-1.outputs.passed != 'true' }}
    needs:
      - tests-shard-4-attempt-1
    continue-on-error: true
    runs-on: warp-macos-15-arm64-6x
    timeout-minutes: 10
    outputs:
      test_started: ${{ steps.unit-test-shard.outputs.test_started }}
      completed: ${{ steps.unit-test-shard.outputs.completed }}
      passed: ${{ steps.unit-test-shard.outputs.passed }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive
      - name: Run unit test shard
        id: unit-test-shard
        uses: ./.github/actions/run-unit-test-shard
        with:
          shard_index: "3"
          shard_count: "6"

  tests-shard-4:
    if: ${{ always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs:
      - tests-shard-4-attempt-1
      - tests-shard-4-attempt-2
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Verify unit test shard passed
        run: |
          scripts/verify_retry_result.sh \
            "Unit test shard" \
            "${{ needs.tests-shard-4-attempt-1.result }}" \
            "${{ needs.tests-shard-4-attempt-1.outputs.completed }}" \
            "${{ needs.tests-shard-4-attempt-1.outputs.passed }}" \
            "${{ needs.tests-shard-4-attempt-2.result }}" \
            "${{ needs.tests-shard-4-attempt-2.outputs.completed }}" \
            "${{ needs.tests-shard-4-attempt-2.outputs.passed }}"

  tests-shard-5-attempt-1:
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    continue-on-error: true
    runs-on: warp-macos-15-arm64-6x
    timeout-minutes: 10
    outputs:
      test_started: ${{ steps.unit-test-shard.outputs.test_started }}
      completed: ${{ steps.unit-test-shard.outputs.completed }}
      passed: ${{ steps.unit-test-shard.outputs.passed }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive
      - name: Run unit test shard
        id: unit-test-shard
        uses: ./.github/actions/run-unit-test-shard
        with:
          shard_index: "4"
          shard_count: "6"

  tests-shard-5-attempt-2:
    if: ${{ always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && needs.tests-shard-5-attempt-1.outputs.completed != 'true' && needs.tests-shard-5-attempt-1.outputs.passed != 'true' }}
    needs:
      - tests-shard-5-attempt-1
    continue-on-error: true
    runs-on: warp-macos-15-arm64-6x
    timeout-minutes: 10
    outputs:
      test_started: ${{ steps.unit-test-shard.outputs.test_started }}
      completed: ${{ steps.unit-test-shard.outputs.completed }}
      passed: ${{ steps.unit-test-shard.outputs.passed }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive
      - name: Run unit test shard
        id: unit-test-shard
        uses: ./.github/actions/run-unit-test-shard
        with:
          shard_index: "4"
          shard_count: "6"

  tests-shard-5:
    if: ${{ always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs:
      - tests-shard-5-attempt-1
      - tests-shard-5-attempt-2
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Verify unit test shard passed
        run: |
          scripts/verify_retry_result.sh \
            "Unit test shard" \
            "${{ needs.tests-shard-5-attempt-1.result }}" \
            "${{ needs.tests-shard-5-attempt-1.outputs.completed }}" \
            "${{ needs.tests-shard-5-attempt-1.outputs.passed }}" \
            "${{ needs.tests-shard-5-attempt-2.result }}" \
            "${{ needs.tests-shard-5-attempt-2.outputs.completed }}" \
            "${{ needs.tests-shard-5-attempt-2.outputs.passed }}"

  tests-shard-6-attempt-1:
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    continue-on-error: true
    runs-on: warp-macos-15-arm64-6x
    timeout-minutes: 10
    outputs:
      test_started: ${{ steps.unit-test-shard.outputs.test_started }}
      completed: ${{ steps.unit-test-shard.outputs.completed }}
      passed: ${{ steps.unit-test-shard.outputs.passed }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive
      - name: Run unit test shard
        id: unit-test-shard
        uses: ./.github/actions/run-unit-test-shard
        with:
          shard_index: "5"
          shard_count: "6"

  tests-shard-6-attempt-2:
    if: ${{ always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && needs.tests-shard-6-attempt-1.outputs.completed != 'true' && needs.tests-shard-6-attempt-1.outputs.passed != 'true' }}
    needs:
      - tests-shard-6-attempt-1
    continue-on-error: true
    runs-on: warp-macos-15-arm64-6x
    timeout-minutes: 10
    outputs:
      test_started: ${{ steps.unit-test-shard.outputs.test_started }}
      completed: ${{ steps.unit-test-shard.outputs.completed }}
      passed: ${{ steps.unit-test-shard.outputs.passed }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive
      - name: Run unit test shard
        id: unit-test-shard
        uses: ./.github/actions/run-unit-test-shard
        with:
          shard_index: "5"
          shard_count: "6"

  tests-shard-6:
    if: ${{ always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs:
      - tests-shard-6-attempt-1
      - tests-shard-6-attempt-2
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Verify unit test shard passed
        run: |
          scripts/verify_retry_result.sh \
            "Unit test shard" \
            "${{ needs.tests-shard-6-attempt-1.result }}" \
            "${{ needs.tests-shard-6-attempt-1.outputs.completed }}" \
            "${{ needs.tests-shard-6-attempt-1.outputs.passed }}" \
            "${{ needs.tests-shard-6-attempt-2.result }}" \
            "${{ needs.tests-shard-6-attempt-2.outputs.completed }}" \
            "${{ needs.tests-shard-6-attempt-2.outputs.passed }}"

  tests:
    if: ${{ always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs:
      - tests-shard-1
      - tests-shard-2
      - tests-shard-3
      - tests-shard-4
      - tests-shard-5
      - tests-shard-6
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Verify unit test shards passed
        run: |
          set -euo pipefail

          SHARD_1_RESULT="${{ needs.tests-shard-1.result }}"
          SHARD_2_RESULT="${{ needs.tests-shard-2.result }}"
          SHARD_3_RESULT="${{ needs.tests-shard-3.result }}"
          SHARD_4_RESULT="${{ needs.tests-shard-4.result }}"
          SHARD_5_RESULT="${{ needs.tests-shard-5.result }}"
          SHARD_6_RESULT="${{ needs.tests-shard-6.result }}"

          if [ "$SHARD_1_RESULT" = "success" ] \
            && [ "$SHARD_2_RESULT" = "success" ] \
            && [ "$SHARD_3_RESULT" = "success" ] \
            && [ "$SHARD_4_RESULT" = "success" ] \
            && [ "$SHARD_5_RESULT" = "success" ] \
            && [ "$SHARD_6_RESULT" = "success" ]; then
            echo "All cmux-unit shards passed."
            exit 0
          fi

          echo "One or more cmux-unit shards failed." >&2
          echo "tests-shard-1=$SHARD_1_RESULT" >&2
          echo "tests-shard-2=$SHARD_2_RESULT" >&2
          echo "tests-shard-3=$SHARD_3_RESULT" >&2
          echo "tests-shard-4=$SHARD_4_RESULT" >&2
          echo "tests-shard-5=$SHARD_5_RESULT" >&2
          echo "tests-shard-6=$SHARD_6_RESULT" >&2
          exit 1

  tests-build-and-lag-attempt-1:
    # Build the full cmux scheme and run the lag regression on WarpBuild.
    # XCUITests cannot run on WarpBuild (Virtualization.framework limitation:
    # XCUIApplication stuck "Running Background", 62s activation timeout per
    # test). Interactive UI tests run via test-e2e.yml on GitHub-hosted runners.
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    continue-on-error: true
    runs-on: warp-macos-15-arm64-6x
    timeout-minutes: 20
    outputs:
      test_started: ${{ steps.lag-regression.outputs.test_started }}
      completed: ${{ steps.lag-regression.outputs.completed }}
      passed: ${{ steps.lag-regression.outputs.passed }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive
      - name: Run lag regression attempt
        id: lag-regression
        uses: ./.github/actions/run-lag-regression

  tests-build-and-lag-attempt-2:
    if: ${{ always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && needs.tests-build-and-lag-attempt-1.outputs.completed != 'true' && needs.tests-build-and-lag-attempt-1.outputs.passed != 'true' }}
    needs:
      - tests-build-and-lag-attempt-1
    continue-on-error: true
    runs-on: warp-macos-15-arm64-6x
    timeout-minutes: 20
    outputs:
      test_started: ${{ steps.lag-regression.outputs.test_started }}
      completed: ${{ steps.lag-regression.outputs.completed }}
      passed: ${{ steps.lag-regression.outputs.passed }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive
      - name: Run lag regression attempt
        id: lag-regression
        uses: ./.github/actions/run-lag-regression

  tests-build-and-lag:
    if: ${{ always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs:
      - tests-build-and-lag-attempt-1
      - tests-build-and-lag-attempt-2
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Verify lag regression result
        run: |
          scripts/verify_retry_result.sh \
            "Lag regression" \
            "${{ needs.tests-build-and-lag-attempt-1.result }}" \
            "${{ needs.tests-build-and-lag-attempt-1.outputs.completed }}" \
            "${{ needs.tests-build-and-lag-attempt-1.outputs.passed }}" \
            "${{ needs.tests-build-and-lag-attempt-2.result }}" \
            "${{ needs.tests-build-and-lag-attempt-2.outputs.completed }}" \
            "${{ needs.tests-build-and-lag-attempt-2.outputs.passed }}"

  ui-display-resolution-regression-attempt-1:
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    continue-on-error: true
    runs-on: warp-macos-15-arm64-6x
    timeout-minutes: 25
    outputs:
      test_started: ${{ steps.display-regression.outputs.test_started }}
      completed: ${{ steps.display-regression.outputs.completed }}
      passed: ${{ steps.display-regression.outputs.passed }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive
      - name: Run display regression attempt
        id: display-regression
        uses: ./.github/actions/run-display-resolution-regression

  ui-display-resolution-regression-attempt-2:
    if: ${{ always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && needs.ui-display-resolution-regression-attempt-1.outputs.completed != 'true' && needs.ui-display-resolution-regression-attempt-1.outputs.passed != 'true' }}
    needs:
      - ui-display-resolution-regression-attempt-1
    continue-on-error: true
    runs-on: warp-macos-15-arm64-6x
    timeout-minutes: 25
    outputs:
      test_started: ${{ steps.display-regression.outputs.test_started }}
      completed: ${{ steps.display-regression.outputs.completed }}
      passed: ${{ steps.display-regression.outputs.passed }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          submodules: recursive
      - name: Run display regression attempt
        id: display-regression
        uses: ./.github/actions/run-display-resolution-regression

  ui-display-resolution-regression:
    if: ${{ always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs:
      - ui-display-resolution-regression-attempt-1
      - ui-display-resolution-regression-attempt-2
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - name: Verify at least one display regression attempt passed
        run: |
          scripts/verify_retry_result.sh \
            "Display resolution regression" \
            "${{ needs.ui-display-resolution-regression-attempt-1.result }}" \
            "${{ needs.ui-display-resolution-regression-attempt-1.outputs.completed }}" \
            "${{ needs.ui-display-resolution-regression-attempt-1.outputs.passed }}" \
            "${{ needs.ui-display-resolution-regression-attempt-2.result }}" \
            "${{ needs.ui-display-resolution-regression-attempt-2.outputs.completed }}" \
            "${{ needs.ui-display-resolution-regression-attempt-2.outputs.passed }}"
