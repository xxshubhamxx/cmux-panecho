interface:
  display_name: "cmux Customization"
  short_description: "Customize cmux actions, UI, layouts, and presets."
  default_prompt: "Use $cmux-customization to run cmux docs workflows --json, choose the matching shipped workflow example, adapt its concrete recipe to the project, validate the config, and save the finished workspace as a reusable layout when useful."
