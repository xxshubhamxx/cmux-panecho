# IROH v2 implementation

Source: [accepted decisions](design/IROH-DECISIONS.md), revision 22, and the user's end-to-end implementation goal. Base: `72ce5e9b41a`, branch `feat-iroh-v2-generation`. Existing primary checkouts and unrelated branches are preserved.

## Required result

Implement the full backend, database, Mac/iOS clients, shared contracts, Dashboard integration, deployment environments, observability and accepted rate limits. Remove incompatible legacy client behavior. Production and staging use the new backend against the existing production database with v2-scoped records. Start with new IROH identity/cache namespaces and preserve existing user authentication.

**Lawrence's adopted decisions are implementation requirements.** Use Zod instead of Ajv for server input and output validation; export JSON Schema and generate Swift/TypeScript models with quicktype. HTTP and socket requests share one local operation handler. Apply immutable Drizzle migrations before serving requests, enforce storage bounds atomically in SQLite, handle storage failures explicitly, and run real workerd migration/persistence tests. Required release checks enforce the Cloudflare boundary and contract compatibility. Deploy in stages with complete, bounded observability. The [adopted rules](design/IROH-DECISIONS.md#accepted-backend-implementation-rules) and [source assessment](design/PR-12199-LESSONS.md) retain the full details and our adaptations, including no scheduled challenge cleanup.

Acceptance requires sustained real simulator use, including relay-only/high-latency traffic, credential rollover without session interruption, multiple workspaces and active Codex work using inexpensive Codex models, launch-to-workspace-list under 2.5 seconds, and resume within 2 seconds after 2 minutes backgrounded. Record one-hour runs against the deployed implementation, with environment, build, connection and timing evidence. The acceptance document supplies the complete matrix.

## Work and evidence

| Area | Status | Required evidence |
| --- | --- | --- |
| Fresh backend and team storage | Handler and storage deployed to development, staging and production | HTTP/socket share TeamBroker. Twenty real workerd tests pass across permissions, native control sockets and SQLite. All three Workers return typed v2 errors for unsupported routes. |
| Cross-team user quotas and ownership | SQLite quotas verified; existing PlanetScale database active | Forty-four ownership records copied and counted. Real database retries succeed and conflicting ownership is rejected. Fresh deployed registration returned 200 after the pool compatibility fix. |
| Versioned Zod contracts and generated Swift/TS | Foundation verified; integration pending | 43 named JSON schemas generate Swift Codable/Sendable and TypeScript models. Generation, TS compatibility checks and remote shared-client tests pass. |
| Mac/iOS integration and legacy removal | IROH owns the live Mac listener; retirement verification continues | The Mac no longer creates an `NWListener` or accepts a legacy TCP socket. Readiness and the observed UDP port come from the native IROH endpoint. A changed preferred port is saved and applied on the next pairing restart, so existing sessions stay up. The iOS v2 composition uses the new identity, control socket and lane types. Live pairing and rollover remain pending. |
| Authentication preservation and fresh IROH state | Pending | Upgrade run preserving sign-in while refusing old IROH trust/cache |
| Pairing opt-in | Default off implemented; live verification pending | Mac uses the current explicit setting in every build and ignores the retired opt-in key. Off/start/disable race checks and onboarding proof remain required. |
| Dashboard | Pending | Team-filtered list/management and stable error behavior |
| Observability | Deployed Axiom ingestion verified; full acceptance pending | Actual registration failures and recovery are queryable. All environments have separate ingest-only tokens. Sentry delivery and complete alert coverage still need verification. |
| Development/staging/production isolation | Workers and Durable Object namespaces deployed | Distinct Worker names, Durable Object namespaces and API ticket keys are configured. Shared PostgreSQL records are scoped by environment/project/team/user; seeded authenticated isolation remains pending. |
| Real simulator acceptance | Pending | One-hour normal/relay-only runs, UI recordings, session counters and performance timings |
| Release checks and PR | Pending | Required checks, source-bound evidence and PR URL |

## Progress

- Initial goal turn: created a clean implementation worktree from fetched main and copied all accepted design documents into it. Previous discussion changed authoritative design state; runtime completion is unproven.
- The older `feat-iroh-v2` branch is a separate transport project and is not the implementation baseline.
- This worktree's current dogfood doctor passes for personal and agent authentication profiles. Shared Swift/iOS verification ran on a leased fleet Mac; that lease was released after evidence capture.
- Backend unit verification: 26 Bun tests pass, covering contracts, HMAC/Ed25519 proofs, Stack authentication, server management authority, request bounds, ticket scope, forged internal headers, output budgets and offline relay signatures. These tests do not prove deployment or live relay compatibility.
- Contract generation preserves separate operations instead of flattening the protocol into optional fields. Bidirectional TypeScript compatibility checks compare representable shapes; runtime array cardinality remains enforced by Zod and exported JSON Schema.
- Real Miniflare/workerd verification: 20 tests and 605 assertions pass across permissions, control sockets and storage. They cover restart persistence, atomic failed enrollment, challenge replacement at capacity, exact expiry rejection, proof replay, receipt invalidation after revocation, shared user output limits across teams, the socket replacement allowance, stale reservation revisions and migration history/rollback failures.
- The shared native suite now passes 48 remote Swift tests in nine suites. It covers control setup, inbound permissions, local QUIC, liveness, credential installation retries and endpoint shutdown. The authentication team-scope test and 13 iOS integration tests also pass remotely. The integrated iOS app compiles; this is not a successful pairing or simulator acceptance run.
- HTTP forwarding creates fresh private headers after authentication; caller headers cannot supply authority to a team object. Every object checks its actual namespace identity. Known device proof is checked on reconnect, and credentials/directories are checked again after asynchronous work that could overlap revocation.
- Socket output is reserved durably per user before sending, including bytes that may still be buffered by Cloudflare. Receipts acknowledge batches of actual output, with no idle messages. Reservations do not expire while a socket may exist; quota pressure checks the owning team's live sockets to recover interrupted setup. Socket replacement accepts and readies the new connection before closing the old one.
- Wrangler defines separate development/staging/production workers and isolated SQLite namespaces. Generated binding types and deployment checks pass. The shared Workers are deployed at `cmux-iroh-v2-development`, `cmux-iroh-v2-staging`, and `cmux-iroh-v2`; their account hostname is `debussy.workers.dev`. The shared PostgreSQL target is selected by `DATABASE_URL`; all three Workers now use the existing production PlanetScale database with environment-scoped v2 records.
- `workers/iroh-v2/scripts/deploy-dev.sh <slug>` now provisions a suffixed development Worker for branch or agent testing. It gives that Worker separate Durable Object namespaces and accepts either `DATABASE_URL` or `PLANETSCALE_DATABASE_URL`, publishing one canonical URL; the shared development Worker remains the latest-main baseline.
- The new IROH v2 workflow invokes the architectural boundary, generated contracts/bindings, type checks, unit tests, real runtime tests and both environment bundle checks. Deploy scripts require these local checks too. The new boundary check passes; GitHub has not run the unpushed workflow yet. Evidence from the current local checks is in [backend logs](evidence/backend-20260910/checks.log).
- September 15: created only the two v2 ownership tables inside the existing production PlanetScale database, copied 30 staging and 14 development reservations from the temporary v2 databases (zero production reservations), checked ownership conflicts and counts, then switched all three Worker secrets. Legacy tables were preserved. Source reservations remain available for rollback; no database was deleted. Local deployment URL entries were updated and their prior values backed up with mode 0600.
- A Mac's directory now includes explicit inbound phone permissions for that Mac. Outgoing visibility never grants inbound access. Real workerd tests cover another user's permitted phone, an unrelated team phone, permission removal, device revocation, pairing disabled, exact permission expiry and pagination across both lists.
- Implementation choice for the previously open offline authority window: at most one hour from verified Stack membership. Each team stores one latest verification lease per user, with a physical 4,096-user guard. Ordinary HMAC requests reuse the original verification time; only fresh Stack verification extends it. Each inbound permission ends at the earlier of the Mac session deadline and phone owner's verification deadline. No scheduled database cleanup or presence heartbeat is added. Prompt membership-removal delivery remains to implement.
- Native control socket tests now load the Wrangler bundle in real workerd, including the PostgreSQL client import graph. HTTP setup/requests, native ping/pong, directory/relay operations and forged-team rejection pass. This does not exercise a live shared database.
- Migration tests now exercise the actual migration function with populated upgrades, repeated activation, missing/future versions, changed migration hashes and a failed upgrade that leaves neither a partial table nor a successful version marker.

- Mac composition now uses the selected Stack team, new installation/key/cache namespaces and the shared v2 control service. Cached listener startup proceeds independently of control setup. Admission uses explicit inbound permissions and a client-owned deadline closes peers whose authority expires. Sign-out and pairing policy fence late callbacks.
- Eight obsolete Mac runtime/observer/dialect/writer files were removed. Shared diagnostics have a separate owner, and Xcode references were updated. The project wiring check passes across 870 test files. The Mac listener migration is now complete: `MobileHostIrxRuntime` reports native IROH readiness, bound UDP port and local socket addresses through one state stream; `MobileHostService` only coordinates policy, status and RPC admission. The old TCP listener, port pre-bind/adoption state, and route-address projection are gone. Settings distinguishes the currently bound port from a saved preference waiting for the next restart. The nine-locale localization audit passes.
- Foreground handling now probes a live connection before replacement, pauses application probes/redial while backgrounded and resumes pending intent. Forty-one shared tests passed before the installation changes; the new combined run passes 48. Loopback recovery measured about 8 ms for a known native closure and 811 ms for a silent peer. Real relay/cellular timings remain unproven. [Liveness evidence](evidence/v2-liveness-20260910/IMPLEMENTATION.md).
- Native relay installation now has one client-owned serialized retry loop, separate from credential minting. Failed installation retries the latest token, and newer credentials interrupt backoff. Endpoint shutdown invalidates pending binds and installation work. Five installer tests and two real endpoint shutdown tests pass. The updated iOS app compiles. [Installation evidence](evidence/v2-relay-install-20260910/IMPLEMENTATION.md).
- September 15: relay PR https://github.com/manaflow-ai/cmux-relay/pull/10 is merged. All seven managed groups use templates that preserve the legacy credential verification key and accept the additional v2 key. The image is pinned to digest `sha256:8ae4ecbdcc4f6783d2e6dbd350a6999b9a756ae7e870133137a80917c2e4e250`. Broker-issued legacy credentials and v2 credentials both pass authenticated relay-only echoes in all seven regions; invalid credentials are rejected. Evidence is in `artifacts/iroh-v2-20260915/relay/{legacy-positive,v2-positive}/`. Terraform state access remains unavailable; a later Terraform apply must preserve these recorded image/key values.
- The remote `cmux-unit` build-for-testing target passes after the listener migration, including the new lifecycle/settings tests. It emits existing project warnings and one pre-existing duplicate test-file build entry, but no v2 compile errors. The fleet GUI preflight is unavailable on the current verification Macs, so test execution remains pending.
- The Mac app then compiled after the v2-only pairing window change. The window now activates the explicit setting, waits for native IROH readiness, and tells the user to sign in on iPhone; it no longer mints a QR or displays Tailscale/direct addresses. The fleet build completed successfully with the existing project warnings.
- The follow-up v2-only pairing cleanup compiled successfully in fleet build `iv2h` (403 seconds, tag `iv2h`) with the offline development-backend mode because the managed backend host was unreachable. The pairing window now treats QR/Tailscale fields as compatibility-only and does not refresh or mint them on v2 state changes.
- The deployed v2 Worker origins are wired into `MobileIrohV2Configuration`. A live unauthenticated route probe against development, staging and production returned the expected typed `404 unsupported_method` response from each Worker.
- The Mac v2 Worker origin was corrected after the first fleet build still showed the retired hostname. Fleet build `iv2m` then compiled the corrected Mac and iOS configuration successfully in 432 seconds. The build was installed remotely without launch; authenticated pairing and long-run simulator acceptance remain unverified.

## Remaining integration requirements

- Complete explicit pairing opt-in verification; remove any remaining callers that actively select legacy attach routes or Stack-bearer transport. The live v2 Mac path is already IROH-only, but shared compatibility declarations remain until their last caller is migrated.
- Verify Mac admission and active-session expiry end to end; implement approved key replacement/recovery and prompt membership-removal delivery.
- Verify real PlanetScale enrollment, relay key rollout and credential installation against actual relays. The inspected relay closes its transport when its token expires, so uninterrupted renewal needs direct verification.
- Complete bounded Axiom/Sentry delivery, durable authority audit behavior at capacity, and all required event/alert coverage. Actual deployed HTTP/socket outcomes now reach Axiom; Sentry delivery and alert coverage remain unproved.
- Enforce migration immutability, contract compatibility and the architectural boundary in required CI checks; finish Dashboard integration.
- Verify foreground recovery against actual relay latency and a two-minute suspension. The pinned native transport has a 30-second idle timeout, so a suspended process may need the permitted reconnect. Complete both deployed one-hour simulator runs and performance acceptance.
- Complete native integration verification for strict local direct mode. The shared endpoint now disables relay transport, and iOS creates that endpoint lazily using the same installation key. Real loopback QUIC tests pass with no relay credentials; automatic IROH mode keeps optional direct upgrades.
- Mac authenticated host status must report the fresh v2 installation device ID used in the directory. Preserve its existing `mac:` namespace prefix for build-compatibility checks; the signed v2 identity uses the raw app bundle ID. iOS projects that prefix only when applying the existing compatibility policy.

## September 15 live verification checkpoint

- Worker runtime source `9c8bbb77a0f`: staging `01384407-7dfe-4990-970a-6ed338558f08`, production `c1f971f3-d321-4987-8908-9bd11578701a`, development `49356be6-9362-4e9c-b58c-9785a706fc02`.
- The existing PlanetScale transaction pool rejects `statement_timeout` as a connection startup parameter (PostgreSQL error `08P01`). The adapter now sets it locally inside each transaction. The real-database regression failed before the fix and passed afterward, including repeated reservation and conflicting-identity rejection. Fresh staging registration succeeded at 03:34:03 UTC. This is a transaction-pool compatibility fix, with the five-second query bound preserved.
- Thirty-six backend unit tests and twenty workerd tests pass. On the overloaded local host, combined runs also hit five-second setup timeouts; the complete twenty-test suite passed with a thirty-second test deadline. The live database regression is a separate opt-in test under `workers/iroh-v2/live/`.
- The clean iOS simulator app is built from `a1f2e8d52c0`, SHA-256 `1f4b14bb380c5c3cfd759cfb43eec159d871c50a25936664c37ce73a60d146f8`, tag `e2va`, simulator `551234B6-0ED0-47ED-B642-A0FFA2505475`. The Mac uses the same source and tag. CI fixes later add the app's explicit IrohLib link dependency.
- A previous simulator paired and exchanged data but used an older dirty artifact. Its brief recording and passive uptime do not satisfy the required engaged hour. The clean simulator's setup was blocked by the database error above. The sustained acceptance run has not passed.
- The clean simulator established same-account RPC after the database fix. The readiness helper incorrectly records the current checkout SHA even when the installed app is older; binary hashes and baked build metadata remain authoritative. The old first-frame timeout on admitted IRX peers is fixed in `5dc1bb65020`; unauthenticated transports retain the fifteen-second limit. A behavior regression waits for that legacy timeout, then verifies the admitted peer can send its first request. New builds and live verification are required.
- A debug-only environment switch, `CMUX_IROH_V2_VERIFY_RENEW_INTERVAL_SECONDS` (30 to 900 seconds), schedules the same API-ticket, relay-credential and directory renewal methods used in production. It exists only in Debug builds for the shortened 15-minute verification and leaves production lifetimes unchanged.
- The configured account rejects `gpt-5.3-codex-spark`. The user requested inexpensive Codex models; the acceptance author had unnecessarily narrowed that to one exact model. The criteria now follow the user request and require the actual selected model and active session evidence.

## Historical Mac build checkpoint

The initial `iv2g` build (`iv2g-dd483ad70d16`, slot `cmux8s-v.2`) compiled and installed without launch. It predates final legacy-file removal and native installation retries. Its wrapper stalled while stopping its lease heartbeat after installation; terminating that owned heartbeat let cleanup finish successfully. No shared tooling source was changed.

The replacement build `iv2g-9fb060193a8f` compiled and installed successfully on `cmux8s-v.2`; its lease was released. All 213 changed source/contract/config paths matched the local overlay, and the build used the correct base revision. [Build receipts](evidence/mac-iv2g-20260910/verification.json). The same wrapper heartbeat stall required terminating its owned loop after signing; cleanup then reported `BUILD_OK`. It uses local mode for the unrelated web development service because the Docker VM was unreachable. IROH itself selects the new Cloudflare origin in `MobileHostV2Configuration`. No app was launched or offered for dogfood.
