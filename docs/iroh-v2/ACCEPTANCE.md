# IROH v2 acceptance and evidence plan

Status: implementation and verification in progress. September 15 clarification: the user requested inexpensive Codex models; the exact Spark-only constraint below was introduced by the acceptance author and has been corrected to that request. Performance, duration and behavior gates remain unchanged. Acceptance author: `/root/v2_acceptance`. Implementation owner: `/root`. Interaction/profiling and final artifact verifier must be separate sessions, named in the final evidence manifest. This document is a completion gate, not a report that anything passed.

## Scope and authority

Implement the entire accepted IROH v2 architecture, Mac and iOS clients, relay integration, Dashboard interactions, storage, contracts, deployment and observability. Preserve Stack sign-in while ignoring previous IROH identities, credentials, grants, directory caches and settings/state that belong to the replaced architecture. New iOS uses v2 exclusively. New Macs must retain older iOS discovery, authorization and connection support on the same IROH endpoint. Older authorization cannot bypass v2 permissions for modern peers. The earlier `feat-iroh-v2` work or an existing IRX transport name is not evidence of this implementation.

The original source receipts below cover revision 21 in `cmuxterm-hq-worktrees/feat-iroh-sequence-diagram/cmux-assets/main/iroh-sequence/`. Source receipts when these criteria were authored:

| File | SHA-256 |
| --- | --- |
| `IROH-DECISIONS.md` | `a457c7b3d3741b9c400ecbc8b5ceb082a0d2184350075aa186126a7a42b90475` |
| `IROH-CAPACITY.md` | `30ebbc0ccf8a477338e4bd670fb9beafa9119c6a82006ae4d109a6f5d2178170` |
| `IROH-OBSERVABILITY.md` | `745adedab21521407ab01635e183d7a3592890372db45628b342aa1deb1b9ee9` |
| `index.html` | `71094f235512abe0dab88915d2b17a148c5dec627b31c48012634b31f6ad6c95` |

The HTML has six target flows: enrollment/control setup, Mac registration and directory updates, peer connection, renewal/recovery, application lifecycle, and backend operation/storage handling. Verify every branch and failure path, not only the successful arrows. The historical main diagram is a reference, not the desired endpoint. Copy the final accepted source documents into the implementation's durable review packet before closeout; record any later revision explicitly.

## September 15 compatibility clarification

The required combinations are new iOS with new Mac, old iOS with new Mac, and continued old iOS with old Mac service compatibility. New iOS with old Mac is not required. This overrides earlier blanket legacy-removal wording. Record the actual app versions for each combination; verify discovery, authorized terminal input/output, revocation and pairing disable. A rejected modern peer must not gain access through older account authorization. Preserve saved-computer metadata across the local database move without importing old trust or broadening account/team/build visibility.

## User acceptance runs

Two complete engaged simulator runs are required, each at least 3,600 seconds of actual foreground use. Use the deployed staging backend for automatic/direct-capable mode and the deployed production backend for relay-only mode. Also run a real-client smoke in staging relay-only and production automatic mode, so both deployments exercise both modes. Production uses only the designated test team and v2-scoped records in the existing production database. Development namespaces remain isolated from production traffic.

Run an isolated tagged Mac and isolated simulator using the configured `agent` Stack profile. Record the leased fleet host, simulator UDID, Mac/iOS bundle IDs, tag, source SHA, built artifact hashes, backend URL, Worker version, database identifiers and relay release. Never use the user's active cmux or a shared simulator. A physical iPhone does not substitute for the explicitly requested simulator run; any repository-required phone handoff is additional and uses the `personal` profile with a matching Mac.

| ID | Scenario and pass condition | Required proof |
| --- | --- | --- |
| U1 | Each run has at least 3,600 engaged foreground seconds. Background intervals and setup are excluded from that total; the wall-clock run is longer. No missing evidence interval or restart silently resets the measurement. | Monotonic run timeline, complete Mac+iOS journals, activity receipts, process identity/liveness records and video timeline. |
| U2 | Create at least five real workspaces during each run; repeatedly switch among them, split terminals, resize, scroll output, edit files, send terminal input and inspect command results. Keep at least three Codex sessions active across different workspaces using an available inexpensive Codex model. Record the exact model and any unavailable preferred model; do not count placeholder or idle sessions. Exercise output and input throughout the run. | Workspace IDs, model selection/session metadata, start/end times, action log and visible successful results. Redact terminal content from backend telemetry. A repeating `date` command alone is insufficient. |
| U3 | No unplanned peer session reconnect, disconnect, endpoint rebuild, admission restart, terminal stream reset or lost/duplicated input during uninterrupted foreground use. API/relay renewal does not change the active peer session or interrupt usable lanes. | Stable connection/session/endpoint-generation IDs on both peers; zero forbidden event counts; terminal input acknowledgments and output cursor continuity; video around every renewal. |
| U4 | Run through real 30-minute relay and one-hour API-ticket lifetimes using production timing, not shortened test lifetimes. Each peer observes at least two relay replacements and one API-ticket replacement. Observe beyond the old API ticket's actual expiry if needed. | Safe issue/expiry/install metadata, credential fingerprints, renewal request IDs, install acknowledgments and old/new overlap intervals. Tokens themselves are excluded. A mint event alone does not prove successful installation. |
| U5 | Relay-only run uses relays for the entire data path. Disable direct traversal/direct fallback and verify the observed path repeatedly and at every connection/path change. | Both endpoint configurations, actual path events, relay connection IDs and aggregate relay bytes. `forceRelay` configuration alone is insufficient. |
| U6 | Exercise a measured high-latency path in the relay-only run. Report actual round-trip latency and the applied network impairment, if any. If the chosen relay is low latency, use an isolated network impairment on the test hosts and record its target and observed distribution. | Relay path proof plus measured median/p95/max round-trip latency before and during load. Label synthetic impairment honestly; relay-only does not by itself prove high latency. |
| U7 | Launch to a rendered, usable workspace list is strictly below 2,500 ms. Measure ten cold process launches per environment, including usable cached credentials, credentials near renewal, and a temporary control-backend delay while cached peer authority is usable. | OS launch boundary, client monotonic milestones, first live workspace response, list-render timestamp and matching video frames for every sample; raw samples, median/p95/max. Merely rendering an old cached list is insufficient. |
| U8 | Background iOS for at least 120 seconds and return. The same workspace and terminal content appear and accept input within 2,000 ms after foregrounding. Prefer retaining the session; any necessary recovery is limited to this labeled scenario and must complete within that bound. | OS/scene background and foreground timestamps, resumed RPC result, input/output receipt, session IDs and video. Perform three cycles per mode, including one crossing a renewal deadline. |
| U9 | First v2 launch with legacy IROH data and preserved Stack sign-in creates v2 trust without using the legacy identity, automatic sign-out or a hidden legacy network fallback. Pairing stays explicitly gated. Subsequent launches reuse the new identity. | Seeded legacy fixtures, before/after scoped storage inventory, endpoint identity comparison, signed-in account snapshot and traced v2-only requests. Measure this launch separately; any required explicit setup step is visible and timed, never silently omitted from performance reporting. |

Keep background recovery and deliberate fault/revocation tests in labeled windows. They do not excuse a disconnect in an uninterrupted foreground window. A make-before-break control-socket replacement is recorded separately from peer reconnection, with the old usable socket retained until the new socket is ready. Credential renewal must not manufacture a new peer connection just to satisfy the counters.

## Exact measurement contract

Each client event includes run ID, source/build identity, process ID, monotonic timestamp, environment, verified team/user opaque IDs, device identity reference, peer reference, connection-attempt ID, peer session ID, endpoint generation, control-socket generation, operation/request ID, event/reason and result. Record wall time for correlation, but never subtract clocks from different machines to compute latency. Use local elapsed time or paired request/response measurements.

Required counters for every run:

- Initial peer admissions, later admissions, peer session ends, automatic redials, endpoint closes/rebinds, explicit revocations and selected-path changes, attributed to both peer and lifecycle window.
- API and relay renewal attempts/successes/failures, successful installs, expiry crossings, old/new overlap and retry delays. Record accidental Stack checks on ordinary operations separately.
- Control socket opens/closes/replacements, request timeouts, HTTP fallbacks, denied/limited operations, directory gaps/resyncs and output-bound hits.
- Terminal lane opens/closes, cursor gaps, input sequence acknowledgments, workspace operations and maximum observed input-to-result delay during renewal versus the same workload outside renewal.
- First dial, peer admitted, first workspace response, list rendered and first usable terminal timestamps for each launch; background/foreground and resumed usable timestamps for each resume.

The run fails if a required counter is absent or stops updating. Video without connection identity cannot prove no reconnects; journal counters without real UI use cannot prove usability. Record clock source, sampling cadence, event loss and log rotation. Screenshots with different hashes are supporting evidence only. Use a stable known workload and sequence markers to detect omitted/duplicated terminal data.

Capture continuous video in bounded segments, dense frame splits around launches/renewals/resumes, contact sheets and representative frames. Preserve every segment's timestamps and join order. Profile the same stimulated path with Instruments or the appropriate platform profiler, recording target PID/tag/UDID and CPU, memory, main-thread stalls, network latency and energy-related repeating work. Include a quiet connected period to demonstrate that no application presence timer prevents DO hibernation.

## Architecture and negative checks

| ID | Requirement | Evidence that must pass |
| --- | --- | --- |
| A1 | New Cloudflare `/v2/` backend and fresh storage; no IROH Vercel runtime dependency. Shared local broker handles HTTP and sockets. | Deployed build/resource inventory, environment bindings, route traces and dependency boundary check actually invoked by release workflow. Exercise every HTTP fallback against v2. Other cmux non-IROH services retain their own routing. |
| A2 | One SQLite DO per environment, Stack project and team. User limits span the user's teams/devices/builds in that environment. | Concurrent requests by the same user through two team DOs spend the same budget; another teammate remains independent; development leaves production untouched. Reconnect, hibernation, schema ID and identity changes do not reset allowances. |
| A3 | Worker verifies Stack user/team/permission; team membership alone never grants terminal access. Scoped HMAC tickets and device proof protect resume. | Missing/forged headers, mismatched team/project/environment/user/device, wrong key, revoked generation, stolen ticket without required proof and insufficient device permission all fail without allocating trusted device state. Valid access works. |
| A4 | One globally unique EndpointID per complete identity tuple. One current device record and one pending challenge slot per identity. | Concurrent first enrollment, retried/lost reply, challenge replacement/expiry/replay, duplicate tuple, duplicate EndpointID, interrupted global reservation and approved key replacement tests. Retry reconciles the same result. No cross-database atomicity assumption. |
| A5 | Challenge lifetime 30 minutes; only first enrollment, approved replacement/recovery need one. API ticket one hour; relay credential 30 minutes. No issuance row per token and no cleanup job for them. | Real clock-boundary tests and row-count assertions after repeated issuance/registration. Expired challenge rejected even while its slot remains. No challenge/credential sweeper or expiry-only alarm. Ordinary launch/wake/renewal leaves registration/challenge counts unchanged. |
| A6 | Returning apps dial with usable cached state concurrently with control setup/refresh. One owner per peer coordinates retries and renewal. | Delay/fail the backend while a cached authorized peer remains reachable; peer connection starts before directory/ticket completion. Concurrent callers deduplicate one peer while separate peers work independently. Failed refresh preserves healthy authorized sessions. |
| A7 | No scheduled application presence heartbeat, JSON ping loop, presence-only alarms or presence-based dial gate. Native transport keepalives remain. | Quiet real workerd socket remains hibernatable; native client ping/pong works; no periodic incoming application frames/storage writes/alarms. Unknown/offline directory labels cannot hide or prevent a cached dial. Test no 90-second idle receive timeout inherited from legacy JSON heartbeats. |
| A8 | Pairing requires explicit Mac opt-in before all IROH networking. Disable invalidates lifecycle before cancellation. | Signed-in but disabled Mac has no IROH endpoint/listener, relay or backend/control/renewal traffic. Disable during each suspended async setup/renewal stage, release late replies, prove no restart. Re-enable intact v2 trust without re-enrollment. Onboarding/empty-state text explains the setting. |
| A9 | Direct addresses remain local/peer; server connection details contain only relay URLs. | Inspect registration/metadata/directory payloads and persisted records under direct mode and IROH optional-direct mode. Direct paths still function locally. No private addresses in backend logs. |
| A10 | Terminal authorization and revocation remain effective during cached/offline operation and overlapping renewal. | Unauthorized team member denied; device owner and explicit authorized peer succeed. Revocation prevents issuance and admission, reaches connected clients, invalidates applicable caches and closes affected sessions. Concurrent old refresh cannot revive authority. Record the chosen maximum stale-authority interval. |
| A11 | Zod validates server request and response boundaries; JSON Schema and quicktype generate Swift/TS types from one versioned source. | Pinned reproducible generation; no drift; TS and Swift compile; valid/invalid shared fixtures; output validation failures handled; schema export fails for unsupported shapes. Clients use generated types, not another runtime schema validator. |
| A12 | Supported v2 request/response behavior survives backend changes. No mandatory startup version exchange. | Oldest supported v2 client/new server and new client/previous supported v2 server matrix; breaking response or meaning has a distinct supported handler. `client_upgrade_required` waits 1h, 6h, then 24h with jitter, explicit retry works, success/app update resets. |
| A13 | Every HTTP status and socket error has explicit client behavior. | Table-driven behavior tests cover 101, successful 2xx/204 as applicable, redirects/unexpected content, malformed response, 400 validation, 401 expiry/auth, 403 permission, 404 missing, 409 identity/revision conflict, 413 size, 429 with retry delay, storage full/quota, unsupported schema, 5xx, timeout, cancellation and socket-close codes. Retry only retryable work and reuse mutation request IDs. Never sign out on a transient server error. |
| A14 | Generous user-based limits and finite buffers protect resources without charging terminal packets or internal calls. | Separate operation buckets; registration 300/hour burst 50; two phones/five Macs fit initial enrollment. 16 KiB inbound frame, 64 KiB snapshot chunks; 1,024 messages/2 MiB per connection, 4,096/8 MiB per user, including transport-buffered bytes. Slow subscriber safely resyncs without losing revocation. No delivered-message or local IROH-attempt quota. |
| A15 | Drizzle schema activation gates traffic; immutable migrations; DB usage constraints reject atomically. | Real Miniflare/workerd persistence, populated upgrade, repeated activation, partial failure rollback, unsupported-schema refusal, compatible code rollback, storage guard and concurrent quota tests. No successful migration marker on failure. Offline devices do not expire from inactivity. |
| A16 | Shared PostgreSQL owns global EndpointID reservation and genuinely shared records; team SQLite owns team-local state. PlanetScale is the current shared PostgreSQL provider. | Unique constraint and recovery tests, one authoritative home per record, no periodic global lookup on relay renewal, plus a migration receipt and live uniqueness check before cutover. Per-user product storage allowance and team physical bounds are explicit and tested. |
| A17 | Dashboard uses verified team directory/settings/revocations and filtered ordered changes. | Two-team/two-user browser tests; permission-filtered list and revisions, changed metadata, revocation, cursor-gap snapshot and team-switch cache isolation. Same operations and limit policy as native clients. |
| A18 | Complete bounded observability works on deployed backends and relays. | Query success/denial/rate-limit/error completion events for HTTP and sockets plus enrollment, renewal, DB, revision, socket and deployment events. Confirm Cloudflare metrics, Axiom ingestion and Sentry test exception. Sink outage never blocks response, memory/time are bounded and loss is visible. Durable authority audit survives export failure. No credentials/bodies/terminal bytes/SQL parameters in records. |
| A19 | Relays validate 30-minute credentials locally using public verification keys and correct audience. | Real relay rejects expired/forged/wrong-audience credentials; key rotation supports still-valid issuances; no backend call per handshake. Logs cover result and aggregate bytes. Load test per-user relay guard separately from exact cross-team backend counters. |
| A20 | Environments, releases and legacy retirement are operationally complete. | New staging/production deployed; shared dev plus isolated branch capability configured; keys/data/budgets separate; canary team and same build promotion receipt; supported schema rollback runbook and smoke. New iOS uses v2 only. New Macs retain the explicitly required older-iOS compatibility service while modern peers require v2 permission. Pre-v2 service shutdown timing is recorded separately. |

For operation limits still labeled proposed in the capacity document, the implementer must record the selected values and tests before claiming A14. Do not silently copy Lawrence's old account-level physical limits onto an entire team. Do not add a generic sweeper because a storage library previously had one. Required telemetry covers all normal operation completions; sampling applies only to optional debug details.

## Decisions that must become concrete before final verification

Record the exact identity tuple and canonical signed bytes; explicit device permission policy; team-switch/publish UX; membership removal delivery and maximum stale-authority interval; offline peer-permission lifetime; global EndpointID reservation/recovery protocol; shared user counter authority and failure behavior; physical storage/memory guards and shared-record attribution; transport ping/failure timing; relay audience and key rotation; retained security-history limits/backups; observability retention/alert thresholds; branch lifetime/deletion policy; and old-service retirement timing. They were open in revision 21. Choosing them is implementation work, not grounds to omit them from completion.

Existing verification is insufficient: `scripts/irx-soak.py` assumes a 15-minute run, a 3,000 ms admission threshold and five-minute relay tokens. It does not measure launch-to-rendered-workspace-list, one-hour tickets, required background recovery, a new backend/resource identity, Zod contracts or this team's permission model. Adapt the behavior harness and inspect its analyzer before trusting a PASS. Source-pattern checks alone cannot replace runtime evidence.

## Final evidence packet

Use one manifest row per criterion/scenario: `criterion -> artifact -> captured source SHA -> Mac/iOS hashes -> backend release -> environment -> run ID -> verdict`. Include baseline/head diff, generated-contract checks, test execution counts, runtime reports, redacted deployment receipts, videos/frame sheets, traces, raw timing samples, network/path proof, operation queries, quota/row-count results, and remaining failures. Pin the exact running artifacts; a newly committed change invalidates prior build evidence under the verification workflow.

A fresh final verifier independently reads the criteria, final diff and artifact packet, with no implementer success narrative. Completion requires every criterion proved, no missing or stale evidence, required checks passing and actionable findings resolved. Deploy authorization does not imply PR merge authorization. Open the PR and present the full evidence; merge follows the user's explicit merge directive.

Dictionary: A soak is sustained active use over a measured period. A monotonic clock measures elapsed time without wall-clock adjustments. A canary deploys to a limited test group first. A migration changes stored database structure. Hibernation lets an idle Durable Object stop executing while its sockets remain open.
