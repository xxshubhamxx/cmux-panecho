/// Layout density for `ChatSessionHeaderView`.
public extension ChatSessionHeaderView {
    enum Style: Sendable {
        /// Standard in-content header layout.
        case regular
        /// Compact two-line layout that fits inside iOS toolbar glass controls.
        case toolbarCompact
    }
}
