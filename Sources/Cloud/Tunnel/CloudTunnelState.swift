import CmuxCloudBannerCore

/// App-target alias for the package-owned tunnel lifecycle.
typealias CloudTunnelState = CmuxCloudBannerCore.CloudTunnelState

/// A snapshot for `vm.tunnel_status` and `cmux vpn status`.
